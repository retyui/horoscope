import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  root: {
    position: 'absolute',
    top: 0,
    right: 0,
    padding: 16,
    fontWeight: '700',
    fontSize: 18,
  },
});

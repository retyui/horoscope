import { makeUseStyles } from '@/features/styles';

export default makeUseStyles({
  root: {
    height: 60,
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
  },
});

import { makeUseStyles } from '@/features/styles';

export default makeUseStyles({
  root: {
    borderRadius: 8,
    alignItems: 'center',
    justifyContent: 'center',
  },
  errorRoot: {
    alignItems: 'center',
  },
  errorText: {
    marginTop: 20,
  },
});
